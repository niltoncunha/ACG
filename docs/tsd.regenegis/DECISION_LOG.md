# REGENEGIS / DECISION_LOG

## Decisões

### D1 — Nome canônico

`REGENEGIS`.

`REGENESIS` fica apenas como trilha histórica de conversa.

### D2 — Versão congelada

`v2.1.2-TEST`.

### D3 — Estado atual

`GO_CONDICIONAL` para teste de campo real. Não é GO final.

### D4 — Destino com Gênio

`niltoncunha/tesserus/docs/tsd.regenegis/`.

### D5 — Formas rejeitadas para esta etapa

- S7-alpha: complexidade alta para ganho insuficiente.
- v2.4-CANON: regressão operacional em simulação.
- v2.1 calibrada/híbrida: ambiguidade de versão e termos não fechados.

### D6 — Patches aceitos em v2.1.2-TEST

- `conflict_index` declarado.
- P5 + P7 simultâneos resolvem para `quarantine`.
- P8 removido.
- `is_hard_noisy` definido.
- TRISEAL no TEST declarado como revisão humana.

### D7 — Regra de não expansão

Antes da bateria de campo real:

- não adicionar etapa;
- não adicionar operador;
- não adicionar lattice interno;
- não adicionar SevenD;
- não renomear componente;
- não reabrir arquitetura.

## Próximo gate

Bateria de campo real controlada.

## Teto decisório

- GO_CONDICIONAL: sim.
- GO final: não.
- CANONICO: não.
