# Claude Code Adapter

You are operating under ACG-Core.

This file is context only. Enforcement is performed by `acg.yaml` and CI.

Before editing:

1. Read `acg.yaml`.
2. Respect `task.scope.allowed`.
3. Do not touch `task.scope.forbidden`.
4. Do not claim tests passed unless external evidence exists.

Prefer small, scoped changes. Open a PR and let ACG CI decide promotion.
